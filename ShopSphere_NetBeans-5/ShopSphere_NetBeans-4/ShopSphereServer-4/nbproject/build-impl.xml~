<?xml version="1.0" encoding="UTF-8"?>
<!--
    IMPORTANT: This file is generated by NetBeans IDE and should not be edited manually.
    ShopSphereServer - Serveur RMI inspire de AgriConnectServer
-->
<project name="ShopSphereServer-impl" default="default" basedir="..">
    <fail message="Please build using Ant 1.8.0+.">
        <condition><not><antversion atleast="1.8.0"/></not></condition>
    </fail>
    <property file="nbproject/private/config.properties"/>
    <property file="nbproject/private/${config}.properties"/>
    <property file="nbproject/private/private.properties"/>
    <property file="nbproject/${config}.properties"/>
    <property file="nbproject/project.properties"/>
    <target name="default" depends="dist" description="Build and dist whole project."/>
    <target name="-pre-init" depends="-pre-pre-init"/>
    <target name="-pre-pre-init"/>
    <target name="-init-private" depends="-pre-init">
        <property file="nbproject/private/private.properties"/>
    </target>
    <target name="compile">
        <mkdir dir="${build.classes.dir}"/>
        <javac srcdir="${src.dir}" destdir="${build.classes.dir}"
               classpath="${javac.classpath}" source="${javac.source}"
               target="${javac.target}" encoding="${source.encoding}"
               includeantruntime="false"/>
    </target>
    <target name="dist" depends="compile">
        <mkdir dir="${dist.dir}"/>
        <jar destfile="${dist.jar}" manifest="manifest.mf">
            <fileset dir="${build.classes.dir}"/>
            <zipgroupfileset dir="lib" includes="*.jar"/>
        </jar>
    </target>
    <target name="run" depends="dist">
        <java classname="${main.class}" classpath="${run.classpath}" fork="true"/>
    </target>
    <target name="clean">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}"/>
    </target>
</project>
